# 可手动选择代理节点的强制代理名单
#
# 优先级：高于所有业务规则（广告拦截、GFWList、服务专属分组等），
# 但低于私有内网直连（GEOIP,private）。
# 命中此名单的域名/IP 走“手动选择”分组，由用户选择具体代理节点。
#
# 支持格式：DOMAIN-SUFFIX / DOMAIN / IP-CIDR / IP-CIDR6，一行一条。
# 维护约定：只加“明确知道、需要手动选择代理节点”的域名/IP。
# 每次加/删，记得在 _fork/CHANGELOG.md 里记一笔。
DOMAIN-SUFFIX,0-0.pro
