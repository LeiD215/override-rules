{
  "name": "override-rules",
  "private": true,
  "version": "2.5.17",
  "description": "Override rules YAML generator",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "build": "npm run typecheck && node scripts/build.mjs",
    "generate": "tsx scripts/yaml_generator/generator.ts",
    "artifacts": "npm run build && npm run generate",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "preversion": "npm run lint && npm run typecheck",
    "version": "node scripts/changelog.mjs && git add CHANGELOG.md",
    "postversion": "git push --follow-tags",
    "record:blackbox": "echo '⚠  记得在 _fork/CHANGELOG.md 追加一条完整记录（开始/结束时间戳、类型、对象、原因、修改、验证、影响、撤回、author），pre-commit hook 会强制检查 src/icons 改动是否有 CHANGELOG 改动伴生。详见 _fork/SOP.md \"硬约束\"章节。'"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.0.0",
    "@typescript/native-preview": "^7.0.0-dev.20260619.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "eslint-config-prettier": "^10.1.8",
    "git-cliff": "^2.13.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1"
  },
  "lint-staged": {
    "*.{ts,js,json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "allowScripts": {
    "esbuild@0.28.1": true
  }
}
